SELECT(item_name) FROM superstore
Order By Price;

SELECT AVG(price) 
FROM superstore;

SELECT MIN(price)
FROM superstore
WHERE category='Kitchen Supplies';

/* what's the total amount of appliances in the superstore */

SELECT SUM(price)
FROM superstore
WHERE category='Appliances';
